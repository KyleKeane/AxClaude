# Normalise line endings: LF in the repository, native on checkout.
* text=auto

# Recorded terminal streams are binary: they contain control bytes and must never be normalised.
*.vt binary

*.png binary
*.ico binary
*.wav binary

# Batch files must keep Windows line endings: cmd.exe misreads some lines with LF alone.
*.cmd text eol=crlf
